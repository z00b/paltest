version: 2
jobs:
  dostuff:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: Greeting
          command: echo Hello, world.

      - run:
          name: Print the Current Time
          command: date

  domore:
    docker:
      - image: debian:stretch

    steps:
      - run:
          name: More things
          command: "false"

workflows:
  version: 2
  build:
    jobs:
      - dostuff
      - domore
